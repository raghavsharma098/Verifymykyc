<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    <style>
        :root {
            --primary: #1a7f5a;
            --primary-dark: #143026;
            --primary-light: #d5fff0;
            --card-bg: #fff;
            --border: #e0e0e0;
            --radius: 18px;
            --shadow: 0 4px 24px rgba(30, 64, 175, 0.08);
        }
        body {
            min-height: 100vh;
            background: linear-gradient(120deg, #f7faff 60%, #e0e7ff 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Inter', Arial, sans-serif;
        }
        .login-container {
            width: 100%;
            max-width: 410px;
            margin: 0 auto;
            padding: 32px 0;
        }
        .admin-card {
            background: var(--card-bg);
            border-radius: var(--radius);
            box-shadow: var(--shadow);
            border: 1px solid var(--border);
            overflow: hidden;
        }
        .admin-card-header {
            background: #1a7f5a;
            color: #fff;
            padding: 28px 32px 18px 32px;
            border-bottom: 1px solid var(--border);
            text-align: center;
        }
        .admin-card-header .admin-icon {
            font-size: 2.2rem;
            margin-bottom: 8px;
            color: #fff;
        }
        .admin-card-header h4 {
            margin: 0;
            font-weight: 700;
            font-size: 1.4rem;
            letter-spacing: 0.5px;
        }
        .admin-card-body {
            padding: 32px 32px 24px 32px;
        }
        .form-label {
            color: #1a7f5a;
            font-weight: 600;
        }
        .form-control {
            border-radius: 8px;
            border: 2px solid #e0e0e0;
            padding: 12px 14px;
            font-size: 1.05rem;
            transition: border-color 0.18s;
        }
        .form-control:focus {
            border-color: #1a7f5a;
            box-shadow: 0 0 0 2px #d5fff0;
        }
        .btn-login {
            background: #1a7f5a;
            color: #fff;
            font-weight: 700;
            border: none;
            border-radius: 8px;
            padding: 12px 0;
            font-size: 1.1rem;
            margin-top: 8px;
            box-shadow: 0 2px 8px rgba(26,127,90,0.10);
            transition: background 0.18s, box-shadow 0.18s, transform 0.12s;
            letter-spacing: 0.2px;
        }
        .btn-login:hover {
            background: #143026;
            box-shadow: 0 0 0 3px #d5fff0, 0 4px 16px rgba(26,127,90,0.18);
            transform: translateY(-2px) scale(1.03);
        }
        .alert {
            border-radius: 8px;
            font-size: 1rem;
        }
        @media (max-width: 600px) {
            .login-container {
                max-width: 98vw;
                padding: 10px 0;
            }
            .admin-card-header, .admin-card-body {
                padding-left: 12px;
                padding-right: 12px;
            }
        }
    </style>
</head>
<body>
    <div class="login-container">
        <div class="admin-card">
            <div class="admin-card-header">
                <div class="admin-icon"><i class="fa-solid fa-user-shield"></i></div>
                <h4 class="mb-0">Admin Login</h4>
            </div>
            <div class="admin-card-body">
                <% if (typeof error !== 'undefined') { %>
                    <div class="alert alert-danger mb-3">
                        <%= error %>
                    </div>
                <% } %>
                <form action="/admin/login" method="POST">
                    <div class="mb-3">
                        <label for="username" class="form-label">Username</label>
                        <input type="text" class="form-control" id="username" name="username" required>
                    </div>
                    <div class="mb-3">
                        <label for="password" class="form-label">Password</label>
                        <input type="password" class="form-control" id="password" name="password" required>
                    </div>
                    <button type="submit" class="btn btn-login w-100"><i class="fa-solid fa-arrow-right-to-bracket me-2"></i>Login</button>
                </form>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 